import { Logger } from "./logger";

export const GatewayService = new Logger("GatewayService");
export const AuthenticationService = new Logger("AuthenticationService");
export const ConfigurationService = new Logger("ConfigurationService");
export const UsersService = new Logger("UsersService");
